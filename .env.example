# Frontend (Vite) config
# This value is baked into the static build.
# Do NOT put secrets here.
#
# Recommended for GitHub Pages:
# Set API base URL in `public/app-config.js` (runtime, no rebuild required).
#
# Optional fallback (dev only):
VITE_API_BASE_URL=http://localhost:3001
